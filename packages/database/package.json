{
  "name": "@repo/db",
  "version": "0.0.1",
  "description": "Database package for Exa-Dashboard",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --skip-generate",
    "db:deploy": "prisma migrate deploy"
  },
  "devDependencies": {
    "prisma": "^6.12.0"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
